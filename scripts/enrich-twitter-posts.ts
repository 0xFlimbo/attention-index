/**
 * pnpm enrich:twitter
 *
 * Stub — the real implementation is scoped to batch B7 (docs/WORKPLAN.md).
 * Referenced by package.json now so the script name exists; do not build the
 * X API enrichment logic here as part of B1.
 */
console.error("enrich:twitter: not implemented yet, see docs/WORKPLAN.md batch B7");
process.exit(1);
